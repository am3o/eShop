# Spring properties
spring:
  application:
     name: authentication-service  # Service registers under this name

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 4201   # HTTP (Tomcat) port
  contextPath: /api/user